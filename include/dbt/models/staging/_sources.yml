version: 2

sources:
  - name: raw
    description: "Raw GA4-style events landed from the generator (one row per event)."
    schema: raw
    tables:
      - name: events
        description: "Faithful copy of the landed parquet; untransformed."
