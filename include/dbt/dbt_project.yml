name: ga4_warehouse
version: "1.0.0"
config-version: 2

profile: ga4_warehouse

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  ga4_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
